<%= form_for(@notice) do |f| %>
  <% if @notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% @notice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ntitle %><br />
    <%= f.text_field :ntitle %>
  </div>
  <div class="field">
    <%= f.label :nfilenumber %><br />
    <%= f.text_field :nfilenumber %>
  </div>
  <div class="field">
    <%= f.label :ncontent %><br />
    <%= f.text_area :ncontent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
