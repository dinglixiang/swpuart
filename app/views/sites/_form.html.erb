<%= form_for(@site) do |f| %>
  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% @site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sname %><br />
    <%= f.text_field :sname %>
  </div>
  <div class="field">
    <%= f.label :sdescribe %><br />
    <%= f.text_area :sdescribe %>
  </div>
  <div class="field">
    <%= f.label :sstate %><br />
    <%= f.text_field :sstate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
